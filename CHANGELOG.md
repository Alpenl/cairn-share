# 更新日志

## 0.3.0

- App 同步 D1 的 AI 标题、摘要、双语正文、图片、相关链接、分类和人工整理结果；支持标签/来源/状态/时间筛选、全文搜索和恢复自动分类。
- 公共 API 用 `include=enrichment` 显式提供增强字段，列表只传摘要，详情按需读取；旧客户端六字段契约保持兼容。
- 新增迁移 0008：富化及整理更新在同一事务内失效列表/详情缓存，避免 App 显示旧内容。
- 删除纯转发的 LinkRepository 和全量列表聚合接口，列表每页到达即可阅读；复用相同版本正文，图片按需读取并限制解码尺寸与缓存。
- 分享弹层支持滚动，软键盘打开或多链接选择时仍可访问保存操作。
- 增加跨端 Worker 集成、Android 单元/设备回归及逐项消融实验。

本版本使用原有签名，可通过应用内“检查更新”升级。后端需先应用截至 0008 的全部 D1 迁移并部署配套 Worker。

## 0.2.7

- 新增持久化本地待上传队列。新增链接会先保存在设备上，断网、超时、服务异常或 Token 不可用时不再丢失。
- 每次打开应用会自动重试待上传链接；设置页可查看队列、逐条重试、全部重试或移除任务。
- 上传任务使用客户端幂等 ID，网络中断后的重复请求不会在云端生成重复链接。
- 检查更新页面会显示新版本的完整更新内容，再由用户决定是否下载并安装。
