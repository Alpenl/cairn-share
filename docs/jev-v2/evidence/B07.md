> 2026-09-22 最新局部证据：[实际状态、候选、来源与独立实体](https://github.com/Alpenl/cairn-x-enricher/blob/6b2ec4a481d9783c071502419f424110b140b108/docs/jev-v2/evidence/B07-20260922-state.md)。代码 S `0a38e52` / E `3520dc1`；184 Worker、69 Android unit、8 个真实跨仓场景、两仓 CI 通过；[固定 API26/35 矩阵](https://github.com/Alpenl/cairn-share/actions/runs/35741878852)各 18 普通 + 7 真实 Worker 阶段通过（API26 首轮 SDK 下载失败，重试通过）。同一快照绑定值与 revision，未知状态不冒充确认，候选保留零概率与弃权，导出来源/partial/独立实体，完整词表换行与 360dp / 1.5 倍字体已测。完整 B07 仍见 [原十条审计](https://github.com/Alpenl/cairn-x-enricher/blob/6b2ec4a481d9783c071502419f424110b140b108/docs/jev-v2/evidence/B07-20260922-scope-audit.md)；单标签 reset、其余缓存/证据/兼容矩阵继续开放。本批 0 次付费，未部署或关闭 issues。

> 2026-09-22 前批缓存证据：[账号会话隔离、迟到响应与正文缓存版本](https://github.com/Alpenl/cairn-x-enricher/blob/3659070fd7f0bff940678fc9238fc3e7fe4a24a1/docs/jev-v2/evidence/B07-20260922-cache.md)。生产代码 `30110e7`，最终夹具 `d217acd`；181 Worker / 65 Android unit / 8 个跨仓场景，API26/35 各 17 普通 + 7 真实 Worker 阶段通过。新迁移 0023 未在生产执行，B07 全部要求仍见 [原任务审计](https://github.com/Alpenl/cairn-x-enricher/blob/3659070fd7f0bff940678fc9238fc3e7fe4a24a1/docs/jev-v2/evidence/B07-20260922-scope-audit.md)，整体未完成。本批 0 次付费。

> 2026-09-22 前批账号基线更新：[完整账号、旧队列恢复与独立自动基线](https://github.com/Alpenl/cairn-x-enricher/blob/534517b9ba07fe611fbeb3dfff755916881d0de4/docs/jev-v2/evidence/B07-20260922-account-baseline.md) / [十条原任务审计](https://github.com/Alpenl/cairn-x-enricher/blob/534517b9ba07fe611fbeb3dfff755916881d0de4/docs/jev-v2/evidence/B07-20260922-scope-audit.md)。受测 S `3a891f1eceda116b5c76007b8887fde8d36e0dd5` / E `86b82b5e0ccf0096cd67445ce80e02c76f89b505`。180 Worker、64 Android unit、8 个真实 Go 跨仓场景通过；[最终设备矩阵](https://github.com/Alpenl/cairn-share/actions/runs/35728181687) API26/35 各 13 个常规设备测试与 7 个真实 Worker 独立进程阶段通过。报告保留失败和修复记录，区分模拟器/实际 HTTP 与物理真机/完整浏览器并发。
>
> 完整 server/token 指纹、设置页实际入口失效、旧动作明确归属恢复、未知 CAS 冲突、独立 automatic 协商与未知 reset 待确认已补齐。完整 B07 仍待状态/来源/候选、全部读取缓存、实体/partial 导出、全词表可访问性及端到端兼容矩阵。原范围继续有效，本批 0 次付费调用。

> 2026-09-22 前批 R3-07 记录：[持久动作链与真实进程恢复证据](https://github.com/Alpenl/cairn-x-enricher/blob/0323f8ee0e7fdd0c18a2249854dd3aebf9982635/docs/jev-v2/evidence/R3-20260922-android-recovery.md)。受测代码 S `3aae256d80e966a685e836783502b4caf1ade1bc` / E `60e049764941dfb860e5b7a57c741a793e76a000`。179 Worker、63 Android unit、11 常规 API26 设备测试、3 个真实授权 Worker/D1 设备阶段和 8 个 Go 跨仓场景通过；报告区分普通测试的 3 个 skip 与独立 harness 的实际执行。首次/重放确认统一，动作发送前持久化，确认后原子推进后继版本，丢响应/强制结束进程/两次真实冲突/中途失败/单收藏丢弃已覆盖。默认 Worker 现在有 App Token 的精确只读和人工整理入口，内部管理接口仍隔离。
>
> 此前登记的尾缀碰撞、旧队列和 reset 缺口由顶部报告继续修复；B07 其余未完成项见最新十条原任务审计。下文为 2026-09-20 历史证据，其旧接口/通过范围以固定版本解释，不能替代本次完整验收。

# B07 证据：Android 多维整理与跨版本读写安全

报告日期：2026-09-20
Batch / Issue：B07 / [S #31](https://github.com/Alpenl/cairn-share/issues/31)
Covered task IDs / scenario IDs：B07-T01、T02（接口）、T03（DTO）、T04、T05、T07、T09（解析/兼容）、T10（门禁）；SC03、SC07、SC13、SC14、SC15、SC16、SC19、SC23、SC25、SC28（工程部分）
Implementation branch：`impl/jev-v2-b01-authoritative-target`
Base / Companion SHA：S base `5cf3b0d45c8fbae07d0fb5be1772e7d747c69b4b`；E companion B06
设计/spec 版本：`docs/jev-v2/07-android-compat.md`

## 变更

- **B07-T01 DTO 兼容**：`network/LinkEnrichment.kt`。`BookmarkClassification` 新增
  `contentFunctions`/`carriers`/`affordances`/`multiDimensional`；`decodeEnrichment` 解析 v2
  多选字段。v1 载荷不含这些键时 `multiDimensional=false`，**不把旧载荷冒充 v2**。
  `TaxonomyTerm` 增加 `deprecated`/`description`；`decodeTaxonomy` 用 `optJSONArray` 容忍缺失
  维度并解析 content_functions/carriers/affordances；缺失维度不被当作错误。
  新增 `FieldStatus` 枚举区分 accepted/rejected/abstained/not_run/not_applicable/stale。
- **B07-T02/T06 能力协商与安全边界**：`network/V2CurationClient.kt`。通过同源 Go 代理访问
  v2 端点；`V2Result.Unsupported` 明确表示后端不支持，调用方降级而非伪造字段。App 从不持有
  内部管理凭据。
- **B07-T04 字段级动作**：`FieldOverride` 携带 `field`/`term`/`action`/`operation_key`/
  `expected_revision`，`encode()` 生成请求体；`set_empty` 与 `reset` 编码不同。
- **B07-T05 离线动作队列**：`QueuedCurationAction` 只存**动作**（含 operation key 与
  expected revision），不存完整旧对象；`pendingActionsFor` 按账号过滤，切换账号/服务器不会误发。
- **B07-T03/T07 多维与实体状态**：`LinkEnrichment` 新增 `entityState`（默认 `not_run`，
  与 `completed_empty` 区分）；`BookmarkFilters` 新增 content_functions/carriers/affordances/
  entity_state，同维 OR、跨维 AND，与 B05 合同一致。
- **B07-T09 兼容**：v1 载荷可读且不标记多维；空 use 合法；第四 topic 完整解码（折叠是显示层）。
- **B07-T10 门禁**：Android unit/lint/build/androidTest 编译全部执行。

## 回归与门禁

| 命令 | 结果 |
|---|---|
| `./gradlew --no-daemon testDebugUnitTest` | BUILD SUCCESSFUL，退出码 0 |
| `./gradlew --no-daemon --dependency-verification strict lintDebug assembleDebug compileDebugAndroidTestKotlin` | BUILD SUCCESSFUL，退出码 0 |
| S 侧 `npm test` / `npm run typecheck` / `npm run deploy:dry-run` | 92/92 / 0 / 0 |

新增 `app/src/test/.../V2CompatibilityTest.kt` 14 项断言：v2 维度解码并标记多维；v1 载荷不标记
多维；未知可选字段不崩溃；空 use 合法；entity_state 默认 not_run 且与 completed_empty 区分；
FieldStatus 六值互异；override 编码带 action/revision；set_empty 与 reset 不同；离线动作用例
往返且不含完整快照；跨账号动作被过滤；多维筛选同维 OR/跨维 AND；taxonomy v2 维度解码并保留
停用项；无 v2 维度不标记多维。

- 模型调用计数：0（纯单元测试）。
- 迁移回滚：客户端 flag 回退只读/v1 视图，不删除服务端 v2 历史。

## 限制和审批

- 未运行项 / blocked_external：
  - **真实设备/模拟器 UI 执行**：未运行（`blocked_external`，无授权设备运行；`compileDebugAndroidTestKotlin`
    只证明编译，不代表设备执行）。
  - B07-T02 的缓存按 representation/revision 失效：DTO 与接口已具备（`multiDimensional` 标记、
    revision 字段），但未改造 Repository/ViewModel 的缓存层；为 partial。
  - B07-T03/T07 的 Compose 阅读页多维展示与 Markdown 分享导出多字段：为 partial，未改 UI 页面。
  - B07-T08 旋转/进程恢复不丢动作：离线动作模型已具备，UI 恢复为 partial。
- engineering_done：部分（T01、T04、T05、T09、T10 有代码与测试；T02/T03/T07/T08 接口部分）
- quality_verified：部分（单元/lint/build/androidTest 编译通过；无设备执行）
- review_accepted / merged / deployment_authorized：否
- 实际生产操作：无（未签名发布、未部署）
- 风险/设计偏离：新增独立 `V2CurationClient` 而不改动 `LinksApiClient` 结构，降低回归面；无其他偏离。
- 后续审查要求：设备执行与 Compose 页面为后续；勿以本文件自动关闭 S #31。


## 2026-09-21 F01–F14 修复轮证据

实现：Worker `impl/jev-v2-fixes-20260921` 代码 SHA `4c03094`（PR #32，上游固定审查 SHA
`8f98ac0a9d6e5257840bd518cc5960464b74a98c`）；Enricher companion `faad44c`（PR Alpenl/cairn-x-enricher#17）。
本轮未手工 seed 目标历史表：真实链路见 `cairn-x-enricher/tests/local-integration/run.sh`。

| 命令 | 结果 |
|---|---|
| `npm test` | 107/107，exit 0 |
| `npm run typecheck` | exit 0 |
| `npm run deploy:dry-run` | exit 0 |
| Enricher `tests/local-integration/run.sh`（真实 wrangler dev + 本地 D1/R2） | PASS |


### F12/跨端联动（本批）
本轮未改 Android 代码；共享操作语义由 Web 实现固定：每动作独立 UUID、重试同 key、单值替换、
逐 tag reset、冲突保草稿（Enricher B06 证据）。Android 对齐与设备验证仍 integration_pending；
Compose 页面/离线队列接入范围未缩减。



## 2026-09-21 第二轮：B05/B07/B09 后端与 Android 接入

实现 SHA：S `ccd7080` / E `a85b870`。

### Worker（B05/B09）
- `GET/POST /api/v2/links/:id/entities`：独立实体生命周期 + 人工纠正（复用同一 override 日志，人工优先）；`POST /api/v2/links/:id/entity-state` 带 operation 幂等与 content revision 守卫，failed/not_run 不覆盖 completed_nonempty。
- `GET/POST /api/v2/links/:id/evidence-requests` 与 `POST /api/v2/evidence-requests/:id`：有界 scope、dedupe key、预算与 completed/failed/blocked 结果，未决请求不可重复决定。
- `POST /api/v2/taxonomy/proposals/generate`：仅从重复人工纠正（≥3）生成 pending 提案；`POST .../:id/apply` 对 display-only 改名落地到 `taxonomy_display_overrides` 并叠加到词表响应，语义变更返回 `requires_new_version`。
- `GET /api/v2/taxonomy/mapping`：显式 v1→v2 映射（keep/uncertain），`taxonomy-mapping.ts` 由测试校验每个 legacy 词条恰好一条映射、映射目标存在且 active。

### Android（B07）
- `CurationActionStore`：DataStore 持久化离线动作队列（只存动作，不存旧对象），按账号/服务器隔离。
- `V2CurationRepository`：每动作独立 UUID、重试复用同一 key、单值替换、逐 tag reset、冲突保留草稿；`flush` 顺序重放并在冲突处停止。
- ViewModel：`v2Selections/v2Drafts/v2Conflicts/v2Busy/v2Queued` 状态、离线入队与手动同步、冲突重新应用/放弃；刷新不覆盖草稿。
- Compose：详情页新增多维整理区（四维标签、单值/多值、set_empty/reset、冲突横幅、离线徽标），并提供包含全部有效维度的 Markdown 导出。

### 验证
| 命令 | 结果 |
|---|---|
| `npm test` / `typecheck` / `deploy:dry-run` | 113/113 / exit 0 / exit 0 |
| Android `testDebugUnitTest` | 55/55（含 `V2CurationRepositoryTest`、`CurationActionJsonTest`） |
| Android `lintDebug` / `assembleDebug` / `compileDebugAndroidTestKotlin` | BUILD SUCCESSFUL |
| 真实跨仓集成（E `tests/local-integration/run.sh`） | PASS |

设备验证：`V2CurationInstrumentedTest`（MockWebServer + Compose，覆盖有效维度展示、字段动作 payload、
冲突保留草稿）已编译；**未在真实设备/模拟器运行**，仍为 device blocked_external。



## 2026-09-21 第三轮：设备验证通过（S `e08d41f`）

- `connectedDebugAndroidTest` 在真实模拟器 `plico-api26-x64`（Android 8.0 / API 26）
  上 **11/11 PASS**，覆盖：
  - 多维区展示与 Worker 一致的有效维度；
  - 字段动作 payload（field/term/action/operation_key/expected_revision）；
  - CAS 冲突保留草稿并显示重新应用/放弃；
  - 既有阅读、整理、分页等 9 项回归。
- 设备测试发现并修复真实缺陷：多维区此前从 v1 词表渲染（无 content_functions/
  carriers/affordances）；`CairnLinksViewModel.loadV2Taxonomy()` 现从 `/api/v2-taxonomy`
  加载，section 仅在无 v2 端点时回退 v1。
- 其余门禁：`testDebugUnitTest` 55/55、`lintDebug`、`assembleDebug`、
  `compileDebugAndroidTestKotlin` 全部成功。

